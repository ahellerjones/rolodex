- check if username and password exist and match 
  - pass back userId
- check if username exists
  - pass back userId
- create user with password,  
  - pass back userId 
  - pass back error if username already take
- delete user