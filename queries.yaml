type LoginInfo struct { 
	user string `json:username`
	pass string `json:password`
	loginOrSignup bool `json:loginOrSignup`
}

- check if username and password exist 
  params: 
    - LoginInfo struct
  returns:  
    - pass back userId, nill error
    - nil userId, err if does not exist 
- check if username exists
  params: 
    - LoginInfo struct
  returns: 
  - pass back userId, nil error
  - nil username, err if does not exist

  
- create user integer with password,  
  params: 
    - LoginInfo struct
  returns: 
    - pass back userId, nil error 
    - nil userId pass back error if username already take
- delete user
  params: 
    - LoginInfo struct
  returns: 
    - err if user does not exist


type Contact struct { 
  name string `json:name`
  address string `json:address`
  phoneNumber string `json:phoneNumber`
  email string `json:email`
  birthday string `json:birthday`
}
- postContact
  params:
    - Contact struct
  all of the fields are optional (going to want to check to make sure theyre not nil?)
  returns: 
  - pass back Id, nil err
  - nil Id, err if contact already exists

- getContacts
    # Get all contacts 
    params: 
    returns:
    returns an array with the contact structs, err if problems. 
