- USERS schema
{
  'CREATE TABLE users(
    userID AUTO_INCREMENT primary key,
    login_name varchar(64) unique not null,
    password varchar(256) not null
    )'
}

- CONTACTS schema
{
  'CREATE TABLE contacts(
    contactID AUTO_INCREMENT primary key,
    userID int, 
    name varchar(256),
    address varchar(256),
    phone_number varchar(64), 
    email varchar(256), 
    birthday varchar(256),
    FOREIGN KEY (userID) REFERENCES users(userID) ON DELETE CASCADE
)'
}
###
###
###
---------------------------------------------------------------------------------------
###
###
###
- check if username and password match
{
  'SELECT userID FROM users
   WHERE login_name = {arg_login_name} AND password = {arg_password}'
}

- check if user exists (returns nothing if not found, returns userID if found)
{
  'SELECT userID FROM users
   WHERE userID = {userID}'
}

- inserts new user on sign up

{
    'INSERT INTO users VALUES(
    DEFAULT, login_name, password)'
}
